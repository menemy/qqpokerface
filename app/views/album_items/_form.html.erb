<%= form_for([@album, @album_item], :html => { :multipart => true }) do |f| %>
  <% if @album_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album_item.errors.count, "error") %> prohibited this album_item from being saved:</h2>

      <ul>
      <% @album_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
